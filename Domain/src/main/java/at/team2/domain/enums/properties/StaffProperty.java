package at.team2.domain.enums.properties;

import at.team2.domain.interfaces.DomainEntityProperty;

public enum StaffProperty implements DomainEntityProperty {
    ID,
    FIRST_NAME,
    LAST_NAME,
    BIRTH_DATE,
    ADDRESS,
    EMAIL,
    ACCOUNT
}